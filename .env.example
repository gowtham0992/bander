# Optional. The canonical judge flow uses deterministic Draft fixtures without this.
OPENAI_API_KEY=

# Optional Telegram hero flow. Keep real values only in ignored local .env.
OPENCLAW_TELEGRAM_BOT_TOKEN=
BANDER_TELEGRAM_BOT_TOKEN=

# Local-only service ports. Change only if these ports are already occupied.
BANDER_PORT=4310
MOCK_SERVICE_PORT=4311
WEB_PORT=4312
