# Bander recording plan

Target: 2:35–2:50, one continuous narrated public video. The film uses the real product. The deterministic browser sandbox appears only as a clearly labelled fallback.

## Before recording

1. Stage only fictional events in the dedicated Google account; confirm `America/Denver` and the intended starting interval.
2. Pair Gil’s phone, keep it outside the protected group, and confirm both phones are ready.
3. Remove “Test” from the group and bot presentation. Keep OpenClaw visually assistant-like and Bander visibly teal/protective.
4. Stop stale gateways and run only `npm run real`. Confirm real Google, live Sol, Telegram binding, and exactly four Bander tools.
5. Keep tokens, OAuth files, IDs, private Calendar content, terminals with environment output, and personal notifications off camera.

## Shot plan

### 0:00–0:18 — the parent promise

Show the two Telegram identities.

> OpenClaw is the assistant my parents can talk to naturally. Bander holds the action credentials, shows the exact deal, and reports what really happened.

### 0:18–0:38 — ask without an approval toll

Ask, `What’s on my calendar tomorrow?` Show the warm schedule answer and the absence of a Bander Card.

> Harmless reading flows freely. Bander returns only a bounded schedule view to OpenClaw—never Google credentials, IDs, ETags, Cards, or callbacks.

### 0:38–1:25 — one compound family deal

Say, `Move Bander Demo Appointment to July 18 at 4 PM and let my son know.` Hold on the one Card long enough to read the complete Calendar interval and exact Gil update. Tap **Do exactly this**. Show the real Google change, Gil’s lock-screen buzz, then Bander’s combined outcome.

> Live GPT-5.6 Sol extracts only bounded hints. Deterministic Bander code finds the eligible real event, preserves its duration, binds the exact paired contact, and renders the family text. One tap approves both effects. Calendar commits first; only then can Bander send the exact text shown on the Card.

### 1:25–1:58 — changed world stops both effects

Prepare a second Card. Change the real event independently in Google, then approve. Show Bander’s refusal and confirm Gil receives nothing.

> Approval is not a blank check. Google’s ETag changed, so Bander refuses the stale plan. It performs no Bander Calendar write and sends no family update.

### 1:58–2:18 — the four-tool boundary

Show a sanitized four-tool inventory and the architecture diagram.

> This protected OpenClaw exposes exactly four bounded Bander tools: capabilities, schedule read, action proposal, and minimal receipt status. There is no messaging tool. OpenClaw never receives Google or Bander Telegram credentials and cannot press approval for itself.

### 2:18–2:42 — Codex red→green and Sol’s real role

Show the selected `calendar_outcome_ambiguous` ledger entry and the green verifier summary.

> Codex helped build Bander in verified slices. One important red test caught a dangerous sentence: after a lost Google response, Bander used to say nothing changed. The fix preserves the unknown result and never sends the family update. Sol handles conversation and bounded interpretation; deterministic code still owns matching, authority, execution, and truth.

### 2:42–2:50 — close

> Bander: ask freely, and approve the exact consequences.

## Guardrails

- Keep the real and deterministic paths unmistakably separate.
- Do not claim atomic distributed transactions, proof of human reading, restart-durable production authority, an installer, standing real autonomy, email, purchases, reservations, payments, or smart-home control.
- Do not claim zero OpenClaw network access or protection outside the dedicated profile.
- Show only human dates and times. End by 2:50.
